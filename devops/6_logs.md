### 멋사 백엔드 스터디 · 로그 관리 편

> 지난 편에서 앱이 죽으면 알림이 오게 만들었습니다. 이제 새벽에 앱이 죽어도 폰이 울리죠.
> 그런데 알림을 받고 나면 다음 질문이 바로 나옵니다. "그래서 왜 죽었는데?"
> 원인을 찾으려면 결국 서버에 들어가 `docker compose logs`를 손으로 뒤지고 있어요. 로그가 쌓이고, 컨테이너가 여러 개가 되면 이게 금방 지옥이 됩니다. 게다가 로그를 방치하면 디스크가 꽉 차서 서버가 통째로 뻗기도 해요.
>
> 오늘은 로그를 제대로 다룹니다. 목표: 로그가 디스크를 안 잡아먹게 관리하고, 필요할 때 빠르게 찾아보기.

---

## Part 0. 오늘 할 일 지도

```
[문제 1] 로그가 무한정 쌓여 디스크가 꽉 참      -> 로그 로테이션으로 자동 정리
[문제 2] logs를 손으로 뒤지기 힘듦             -> 필터링 기술 + 웹 뷰어
[문제 3] 컨테이너가 여러 개라 한눈에 안 보임    -> 통합 로그 도구
```

오늘의 흐름:

1. `docker logs`의 기본기와 한계 이해하기
2. 로그 로테이션 설정 (디스크 꽉 참 방지 - 제일 중요)
3. 로그 빠르게 찾아보는 기술
4. 웹에서 편하게 보는 도구 소개 (Dozzle)
5. 제대로 된 로그 수집 스택 맛보기 (Loki + Grafana)

> 팁: 오늘은 화려한 결과물보다 "안 하면 언젠가 서버가 죽는" 실무 필수 편이에요. 특히 Part 2(로테이션)는 안 하면 진짜로 사고가 납니다.

---

## Part 1. docker 로그의 기본기와 한계

지금까지 우리가 쓴 명령을 다시 보죠.

```bash
# 특정 서비스 로그를 실시간으로 보기
docker compose logs -f app
```

이게 왜 한계가 있을까요? 몇 가지 유용한 옵션과 함께 짚어봅니다.

```bash
# 최근 100줄만 보기 (전체 다 쏟아지는 걸 막음)
docker compose logs --tail 100 app

# 시간 표시와 함께 보기 (언제 무슨 일이 있었는지)
docker compose logs -t app

# 최근 10분 로그만 보기
docker compose logs --since 10m app

# 여러 조건 조합: app 서비스의 최근 30분, 시간표시, 마지막 50줄
docker compose logs --since 30m -t --tail 50 app
```

한계가 보이나요?

- 로그가 수만 줄 쌓이면 원하는 걸 눈으로 찾기 거의 불가능
- 컨테이너가 여러 개(app, db, caddy...)면 각각 따로 봐야 함
- 그리고 가장 위험한 것: **이 로그들이 서버 디스크에 계속 쌓입니다.** 관리 안 하면 무한정.

Part 2부터 이걸 하나씩 해결해요.

---

## Part 2. 로그 로테이션 - 디스크 꽉 참 방지 (필수)

이게 오늘 편에서 제일 중요합니다. 실무에서 초보자가 겪는 대표 사고 중 하나가 "**로그가 디스크를 다 채워서 서버가 멈춤**"이에요.

### 왜 위험한가

도커는 기본적으로 컨테이너 로그를 **파일로 계속 쌓아둡니다.** 삭제를 안 해요. 트래픽이 많거나 앱이 에러를 뿜기 시작하면 이 파일이 몇 GB까지 커지고, 2GB짜리 우리 Lightsail 디스크가 꽉 차면 앱이 아니라 서버 자체가 이상해집니다.

### 현재 디스크 상태 확인

```bash
# 디스크 사용량 확인 (Use% 가 90%를 넘어가면 위험)
df -h

# 도커가 잡아먹는 용량 확인
docker system df
```

### 해결: 로그 자동 정리 설정

`docker-compose.yml`에서 각 서비스에 **로그 크기 제한**을 걸어줍니다. "로그 파일 하나가 10MB를 넘으면 새 파일로 넘기고, 파일은 최대 3개까지만 유지" 하는 설정이에요.

```yaml
services:
  app:
    build: .
    depends_on:
      - db
    env_file:
      - .env
    logging:                    # 여기 추가
      driver: "json-file"
      options:
        max-size: "10m"         # 로그 파일 하나 최대 10MB
        max-file: "3"           # 파일 최대 3개 (합쳐서 최대 30MB)

  db:
    image: mysql:8
    logging:                    # db에도 동일하게
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  caddy:
    image: caddy:2-alpine
    # ... 기존 설정 ...
    logging:                    # caddy에도
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

적용:

```bash
docker compose up -d
```

> 이 설정 하나로 각 컨테이너 로그가 최대 30MB로 묶여서 자동 관리됩니다. 오래된 로그는 알아서 지워져요. 스터디원 전원이 반드시 넣게 하세요. 안 넣으면 몇 주 뒤에 누군가는 서버가 멈춥니다.

### 이미 쌓인 로그 청소 (필요시)

```bash
# 안 쓰는 컨테이너/이미지/네트워크/빌드 캐시 정리
docker system prune -f
```

---

## Part 3. 로그 빠르게 찾아보는 기술

로그를 손으로 볼 수밖에 없는 상황도 많죠. 이때 유용한 기술들입니다.

### 특정 단어만 골라 보기 (grep)

```bash
# 로그에서 ERROR 가 들어간 줄만 뽑기
docker compose logs app | grep ERROR

# 대소문자 무시하고 exception 찾기
docker compose logs app | grep -i exception

# 에러 줄과 그 앞뒤 3줄까지 같이 보기 (맥락 파악용)
docker compose logs app | grep -i error -A 3 -B 3
```

> `-A 3`은 뒤(After) 3줄, `-B 3`은 앞(Before) 3줄. 에러는 앞뒤 맥락이 중요해서 이게 유용해요.

### 로그를 파일로 저장해서 분석

```bash
# 최근 로그를 파일로 저장 (나중에 천천히 보거나 공유용)
docker compose logs --since 1h app > ~/app-log.txt
```

### 실시간으로 보면서 특정 단어만

```bash
# 실시간 로그 중 ERROR 만 흘려보기
docker compose logs -f app | grep --line-buffered ERROR
```

> `--line-buffered`를 붙여야 실시간 로그에서 grep이 즉시 반응해요. 안 붙이면 버퍼에 모였다가 늦게 나옵니다.

이 정도면 급할 때 대응은 되지만, 여전히 터미널에서 명령을 쳐야 하죠. Part 4에서 이걸 웹 화면으로 옮깁니다.

---

## Part 4. 웹에서 편하게 - Dozzle

지난 편의 Uptime Kuma 기억나죠? 로그에도 비슷하게 **웹 화면에서 실시간 로그를 예쁘게 보여주는** 가벼운 도구가 있어요. Dozzle입니다.

특징:

- 도커 컨테이너 로그를 **웹 브라우저에서 실시간**으로 봄
- 설정 파일 필요 없음, 컨테이너 하나만 띄우면 끝
- 여러 컨테이너를 한 화면에서 전환하며 봄, 검색창도 있음
- 로그를 따로 저장하진 않음 (실시간 뷰어라는 성격)

`docker-compose.yml`에 추가:

```yaml
  dozzle:
    image: amir20/dozzle:latest
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock   # 도커 로그를 읽기 위한 연결
    environment:
      - DOZZLE_NO_ANALYTICS=true
    # Caddy 뒤에 붙여 logs.내도메인.com 같은 주소로 접근
```

> `/var/run/docker.sock`을 연결해주면 Dozzle이 서버의 모든 컨테이너 로그를 읽을 수 있어요. 이 소켓은 도커를 조종할 수 있는 강력한 통로라, 외부에 함부로 열면 안 됩니다. 반드시 Caddy 뒤에 두고, 필요하면 기본 인증(아이디/비번)을 걸어 접근을 제한하세요.

> Uptime Kuma가 "상태 감시 + 알림"이라면 Dozzle은 "로그 실시간 뷰어"예요. 역할이 다르니 둘 다 띄워두면 대시보드가 꽤 그럴듯해집니다.

---

## Part 5. 제대로 된 로그 수집 스택 맛보기 (Loki + Grafana)

Dozzle은 "지금 흘러가는 로그"를 보는 데 최고지만, "3일 전 새벽 2시 로그를 검색"하는 건 못 해요. 로그를 **오래 저장하고 검색**하려면 전용 스택이 필요합니다. 실무에서 많이 쓰는 조합이 Loki + Grafana예요.

개념만 잡고 갑니다. (실습은 나중 심화 편에서)

```
[컨테이너 로그] -> [Promtail: 로그 수집기] -> [Loki: 로그 저장·검색] -> [Grafana: 화면·검색 UI]
```

- **Promtail**: 각 컨테이너 로그를 긁어모아 Loki로 보냄
- **Loki**: 로그를 저장하고 빠르게 검색하게 해주는 저장소
- **Grafana**: 로그를 그래프와 검색창이 있는 웹 대시보드로 보여줌

> 이건 "로그를 며칠~몇 주 보관하고, 조건 검색하고, 시각화까지" 하는 본격적인 구성이에요. 우리 스터디의 2GB Lightsail에는 다소 무거울 수 있어서, 오늘은 "이런 세계가 있다"만 알아두고 넘어갑니다. Dozzle + 로그 로테이션만 제대로 해도 지금 단계에선 충분해요.

정리하면 단계는 이렇습니다.

- 지금 우리: 로그 로테이션(필수) + grep(급할 때) + Dozzle(웹 뷰어)
- 다음 단계: Loki + Grafana (오래 저장하고 검색해야 할 때)

---

## Part 6. 자주 나는 에러 & 해결법

**증상: `df -h`에서 디스크가 거의 꽉 참 (90%+)**
원인: 로그 로테이션 미설정 또는 안 쓰는 도커 이미지 누적.
해결: Part 2의 `logging` 설정을 넣고, `docker system prune -f`로 정리. 그래도 안 되면 `docker system df`로 뭐가 용량을 먹는지 확인.

**증상: `logging` 설정을 넣었는데 로그 크기가 안 줄어듦**
원인: 이미 쌓인 기존 로그에는 소급 적용이 안 됨. 설정은 새 로그부터 적용됨.
해결: 컨테이너를 재생성(`docker compose up -d`)하고, 기존 로그는 `docker system prune`으로 정리.

**증상: Dozzle 화면에 컨테이너가 안 보임**
원인: `/var/run/docker.sock` 볼륨 연결 누락.
해결: docker-compose의 dozzle 서비스에 소켓 볼륨이 정확히 들어갔는지 확인 후 재생성.

**증상: grep을 실시간 로그(`-f`)에 걸었는데 아무것도 안 나옴**
원인: 버퍼링 때문에 즉시 안 나오는 것.
해결: `grep --line-buffered` 로 실행. 그리고 찾는 단어의 대소문자를 확인(`-i` 옵션).

**증상: Dozzle 주소가 외부에 그대로 노출됨**
원인: Caddy 뒤에 두지 않고 포트를 직접 열었거나, 인증을 안 걸었음.
해결: Dozzle 컨테이너에 `ports`로 직접 포트를 열지 말고 Caddy를 통해서만 접근하게 하고, 기본 인증을 추가. 로그에는 민감 정보가 담길 수 있어 공개 노출은 위험함.

---

## 오늘의 3줄 요약

1. 로그 로테이션은 선택이 아니라 필수다. `docker-compose.yml`의 각 서비스에 `logging` 옵션(max-size, max-file)을 넣어 로그가 디스크를 잡아먹지 않게 한다. 안 하면 언젠가 서버가 멈춘다.
2. 급할 땐 grep으로 찾는다. `docker compose logs app | grep -i error -A 3 -B 3` 처럼 조건과 앞뒤 맥락을 조합해 원하는 로그를 빠르게 찾는다.
3. 웹으로 편하게 볼 땐 Dozzle, 오래 저장하고 검색해야 할 땐 Loki + Grafana. 지금 단계에선 로테이션 + Dozzle로 충분하다.

이제 여러분 서버는 로그 때문에 뻗을 걱정이 없고, 문제가 생겼을 때 원인을 빠르게 추적할 수 있습니다.

---

### 다음 편 예고

앱도 감시하고 로그도 관리되는데, 정작 제일 중요한 게 아직 무방비입니다. 바로 데이터베이스예요. 서버가 실수로 삭제되거나 디스크가 망가지면 그동안 쌓인 데이터가 통째로 날아갑니다. 다음 편에서는 DB를 자동으로 백업하고, 필요할 때 되살리는 방법을 다룹니다.
